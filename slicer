Private Sub Worksheet_FollowHyperlink(ByVal Target As Hyperlink)

    Dim fileName As String
    Dim sc As SlicerCache
    Dim si As SlicerItem
    
    'BJ column = 62 (agar alag hai to change karein)
    fileName = Trim(Me.Cells(Target.Range.Row, 62).Value)
    
    Application.ScreenUpdating = False
    
    Set sc = ThisWorkbook.SlicerCaches("Slicer_File_name1")
    
    sc.ClearManualFilter
    
    'Data Model slicer ke liye
    For Each si In sc.SlicerCacheLevels(1).SlicerItems
        si.Selected = (si.Name = fileName)
    Next si
    
    Sheets("View").Activate
    
    Application.ScreenUpdating = True

End Sub
